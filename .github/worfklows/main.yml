# /.github/worfklows/main.yml
name: Trigger for Daily Maintenance Build
on:
  schedule:
    - cron: '0 0 * * *'
jobs:
  build:
    name: Daily maintenance Build
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/608164da29990e22e640be28
